    import XCTest
    @testable import VoiceMemoView

    final class VoiceMemoViewTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(VoiceMemoView().text, "Hello, World!")
        }
    }
